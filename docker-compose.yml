services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      ROLEGREP_DATA_DIR: /app/data
    volumes:
      - rolegrep-data:/app/data
    restart: unless-stopped

volumes:
  rolegrep-data:
